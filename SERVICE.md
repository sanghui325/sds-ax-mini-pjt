# SERVICE · 해외여행 플랜 Agent

## 1. 사용자·문제·가치
- 누구를 위한 서비스인가: 아시아 해외여행을 계획하는 직장인
- 어떤 문제를 푸는가: 여행지 추천·일정(항공/숙박/교통/예산)·주의사항(비자, 반입금지물품, 치안 등) 정보가 블로그·커뮤니티·유튜브 등 여기저기 흩어져 있어 종합하기 어렵다
- 지금은 어떻게 해결하나: 블로그·커뮤니티·유튜브를 하나씩 직접 찾아보며 정보를 모은다
- 이걸로 뭐가 좋아지나: 여행 기간·목적·예산 조건을 말하면 여행지 추천부터 일정, 주의사항까지 한 번에 근거와 함께 받는다

## 2. 서비스 확장 관점
- 기여: 여행 계획을 위한 정보 탐색·종합 시간을 줄인다
- 규모: 과제 평가용으로 몇 번 실행
- 대체제와 차별점: 블로그·유튜브는 정보가 흩어져 있고 최신성·신뢰도를 확인하기 어렵다. 이 에이전트는 공식 정보(비자·안전)와 정리된 데이터를 근거(contexts)로 제시하며 한 번에 종합해 답한다

## 3. 사용 예상 도구·데이터
- 도구
  - 여행지 추천: 기간(날씨)·목적(휴양/관광/쇼핑 등)에 맞는 도시 후보 제시
  - 일정·예산 플래너: 총예산을 항공권·숙박·교통·식비 등으로 자동 배분해 기간별 일정 구성
  - 주의사항 조회: 비자, 출입국 반입금지물품, 소매치기 등 안전 정보 안내
- 데이터 (data/)
  - 대상 12개 도시: 일본(도쿄/오사카/오키나와), 중국(베이징/상하이/청두), 베트남(하노이/다낭/푸꾸옥), 태국(방콕/치앙마이/푸켓)
  - `data/destinations.json` — 도시 메타데이터 (여행지 추천용)
    - 필드: `country`, `city`, `purpose`(휴양/관광/쇼핑 배열), `season_recommend`(봄/여름/가을/겨울 배열)
  - `data/prices.json` — 구간별 추정 비용, 1인·원화 기준 (일정·예산 플래너용)
    - 필드: `city`, `season`, `flight_price_krw`(min/max), `hotel_price_krw_per_night`(저/중/고), `local_transport_krw_per_day`, `food_price_krw_per_day`(1인 1일 기준)
  - `data/visa_safety.md` — 국가별 주의사항 문서 (RAG retriever 검색 대상)
    - 포함 항목: 비자·무비자 체류 가능 기간, 반입금지물품, 치안·소매치기 주의 지역, 자연재해 주의
  - `data/weather.json` — 도시별 계절 특징 (여행지 추천 필터 + RAG contexts 겸용)
    - 필드: `city`, `season`, `avg_temp_c`, `note`(우기·태풍 등 특이사항)
  - 공통 규칙: 위 파일의 `city` 값은 `destinations.json` 기준 12개 도시명으로 통일해 도구 간 조인이 깨지지 않게 한다
- 입력 형식: `question` 필드에 자연어로 작성, 아래 슬롯을 채워서 보낸다고 가정
  - 여행 기간 (필수): 구체적 날짜(예: "10월 5일~9일") 또는 계절+기간(예: "가을, 5박6일") 둘 다 허용
  - 목적 (필수): 휴양/관광/쇼핑 3개 중 하나 이상 — `destinations.json`의 `purpose` 값과 동일한 닫힌 집합, 자유 텍스트로 와도 이 3개로 매핑
  - 총예산 (필수): 1인 원화 기준 총액 숫자(예: "200만원") — 항목별 분리 입력은 받지 않고 총액만 받는다
  - 위 필수 슬롯(기간/목적/예산) 중 빠진 게 있으면 임의로 가정하지 않고, 기간 → 목적 → 예산 순으로 한 번에 하나씩 되묻는다 (session_id로 이어감)
  - 도시 지정 (선택): 도시를 지정하더라도 추천·일정·주의사항으로 진행

## 4. 서비스 정책 (가드레일 요약)
- 준비된 4개국·도시 목록에 없는 곳은 지어내지 않고 지원 범위를 안내한다
- 비자·안전 등 주의사항은 출처(contexts) 없이 답하지 않는다
- 기간·목적·예산 등 정보가 부족하면 임의로 단정하지 않고 되묻는다 (question 외 session_id로 대화 맥락을 이어감)
- 항공권·숙박·교통·식비 배분 합계는 사용자가 말한 총예산을 넘지 않는다. 최저가로도 총예산을 넘으면 배분을 지어내지 않고 예산 부족을 안내한다

## 5. 성공 기준
- evaluation/test_queries.csv 기반 평가 기준을 12건 중 10건 이상 통과
- 지원 4개국·도시 외 질문에 대해 지어낸 답변 0건

